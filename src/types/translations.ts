export interface TranslationResources {
  theme: {
    enableLight: string
    enableDark: string
  }

  language: {
    label: string
    selectLanguage: string
    portuguese: string
    english: string
  }

  navigation: {
    home: string
    about: string
    process: string
    expertise: string
    stack: string
    projects: string
    contact: string
  }
}